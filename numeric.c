#include<stdio.h>
#include<conio.h>
int main()
{
int num;
printf("enter the numeric values:",num);
scanf('%d",&num);
if(num != 'a'||'z')
{
printf("the given number is numeric:");
}
else(num = 'a'||'z')
{
printf("the given number is not numeric:");
}
return 0;
}
}

